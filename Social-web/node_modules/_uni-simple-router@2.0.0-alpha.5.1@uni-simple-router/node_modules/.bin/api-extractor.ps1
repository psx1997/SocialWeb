#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/node$exe") {
  & "$basedir/node$exe"  "$basedir/../../../_@microsoft_api-extractor@7.12.1@@microsoft/api-extractor/bin/api-extractor" $args
  $ret=$LASTEXITCODE
} else {
  & "node$exe"  "$basedir/../../../_@microsoft_api-extractor@7.12.1@@microsoft/api-extractor/bin/api-extractor" $args
  $ret=$LASTEXITCODE
}
exit $ret
